%% Example 1
clearvars, clc

number = input("Enter the number ranges from 1 to 10: ");

switch number
    case{1,3,5,7,9}
        disp("It is odd");
    case{2,4,6,8,10}
        disp("It is even");
    otherwise
        disp("The number does not match the range 1 - 10")
end

%% Example 2