clearvars, clc

% zeros, ones, eye, rand, diag

x_zeros = zeros(3,4)
x_ones = ones(3,4)
x_eye = eye(3)
x_rand = rand(3, 4) % random numbers between 0 and 1

x = [1, 2, 3, 4, 5]; %diagonal matrix
diag(x)